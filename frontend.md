01- Frontend


The frontend is the service in RoboShop to serve the web content over Nginx.
👉 This is static content, and we need a web server to serve it.
👉 The developer has chosen Nginx as the web server, so we will install the Nginx web server.

ou can list modules by using:-

## Install Nginx
dnf module list ngi
```bash
''' dnf install nginx'''
